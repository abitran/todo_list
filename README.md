# Todo List: A simple TODO list written in C 

## Installation
Run make
## Usage

`./todo` \
`-c "Name of the task" "Description of the task" [dd/mm/yyyy]` \
`-l List the tasks` \
`-d [id] Delete a task`
